# 금고·감사로그·세션. 절대 커밋 금지 — 최우선
data/

node_modules/
dist/
*.tsbuildinfo
.env
.env.*

# launcher scripts hold service keys; only the .example template is tracked
*.cmd
!*.cmd.example

# live tests target the operator's own sites and stay local
backend/test/live/
